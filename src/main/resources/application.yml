server:
  port: 8083

#多数据源配置
spring:
  datasource:
    dynamic:
      primary: master #设置默认的数据源或者数据源组,默认值即为master
      datasource:
        master:
          driver-class-name: com.mysql.jdbc.Driver
          url: jdbc:mysql://127.0.0.1:3306/chemical_rfid?serverTimezone=Asia/Shanghai&characterEncoding=utf8&useSSL=false&allowMultiQueries=true
          username: root
          password: 123456
        slave_1:
          username: fsm
          password: Nju123456
          driver-class-name: com.mysql.jdbc.Driver
          url: jdbc:mysql://rm-uf664b66p5k7i4i7gno.mysql.rds.aliyuncs.com:3306/chemical_qr_code?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai


  thymeleaf:
    mode: HTML5
    cache: false
    prefix: classpath:/templates/
    encoding: UTF-8
    suffix: .html
    check-template-location: false

  servlet:
    multipart:
      max-file-size: 2MB
      max-request-size: 20MB
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
  mvc:
    static-path-pattern: /static/**
    async:
      request-timeout: 5000
# 配置mybatis
mybatis:
  mapper-locations: classpath*:/dao/**Dao.xml
  typeAliasesPackage: com.example.rfid.entity

# 配置pagehelper
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql





